baseurl: /derive-archive
name: Dérive Archive
markdown: kramdown
include: ["_pages"]
gems:
  - jekyll-redirect-from
defaults: 
  -
    scope: 
      path: ""
    values:
      layout: "default"
  -
    scope: 
      path: "_posts"
    values:
      layout: "post"
#theme: jekyll-theme-tactile
